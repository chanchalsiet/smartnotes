from sqlalchemy import create_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker

DATABASE_URL = "postgresql://chanchal:ramu123@localhost:5432/admin" = "postgresql://chanchal:ramu123@localhost:5432/admin"

engin = create_engine(DATABASE_URL)
SessionLocal = session_maker(autocommit = False, autoflush = False, bind = engin)
Base = declarative_base()


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.closed()
